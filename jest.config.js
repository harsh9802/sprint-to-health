export default {
    transform: {
      "^.+\\.js$": "babel-jest",  // Use Babel to transform JavaScript files
    },
    testEnvironment: "node", // Optional, ensures that Node.js environment is used
  };
  